import { Component, OnInit, ViewEncapsulation } from '@angular/core';

@Component({
    // moduleId: module.id,
    selector: 'wscm',
    templateUrl: 'wscm_panel.html',
    styleUrls: ['wscm_panel.css'],
    encapsulation: ViewEncapsulation.None
})

export class WscmPanel implements OnInit {
    ngOnInit() {}
}